config:
  target: "http://localhost:3001"
  phases:
    - duration: 60
      arrivalRate: 50

scenarios:
  - flow:
      - post:
          url: "/api/quote"
          json:
            tokenIn: "WMST"
            tokenOut: "USDC"
            amountIn: "1000000000000000000"
