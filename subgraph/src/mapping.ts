// AssemblyScript subgraph mappings.
// Implement handlers that translate on-chain events into the entities defined
// in schema.graphql once the Factory ABI is generated via `graph codegen`.

// import { PoolCreated } from "../generated/Factory/Factory";
// import { Pool } from "../generated/schema";

// export function handlePoolCreated(event: PoolCreated): void {
//   const pool = new Pool(event.params.pool.toHexString());
//   pool.save();
// }
