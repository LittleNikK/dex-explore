{
  "name": "mstswap-subgraph",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy-local": "graph deploy --node http://localhost:8020 mst/uniswap"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.66.0",
    "@graphprotocol/graph-ts": "^0.32.0"
  }
}
